---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: fluxshardsets.templates.weave.works
spec:
  group: templates.weave.works
  names:
    kind: FluxShardSet
    listKind: FluxShardSetList
    plural: fluxshardsets
    singular: fluxshardset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FluxShardSet is the Schema for the fluxshardsets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FluxShardSetSpec defines the desired state of FluxShardSet
            properties:
              shards:
                description: Shards is a list of shards to deploy
                items:
                  description: ShardSpec defines a shard to deploy
                  properties:
                    name:
                      description: 'Name is the name of the shard TODO: make this
                        required'
                      type: string
                  type: object
                type: array
              type:
                description: 'Type is the type of the deployment, e.g. kustomization,
                  helm, source, notification TODO: make this an enum TODO: make this
                  required'
                type: string
            type: object
          status:
            description: FluxShardSetStatus defines the observed state of FluxShardSet
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
